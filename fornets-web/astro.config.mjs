// @ts-check
import { defineConfig } from 'astro/config';

// GitHub Pages de projecte: https://polruzafa.github.io/fornets
// Si algun dia hi ha domini propi, canvieu `site` i elimineu `base`.
export default defineConfig({
  site: 'https://polruzafa.github.io',
  base: '/fornets',
});
